# UnderwriteIQ frontend

The frontend is a Next.js 16 dashboard for the UnderwriteIQ Federato MVP.

```bash
npm install
npm run dev
```

The app expects the FastAPI backend at `http://localhost:8000`. Copy `.env.example` to `.env.local` to change the API origin.

Available checks:

```bash
npm run lint
npm run build
```
