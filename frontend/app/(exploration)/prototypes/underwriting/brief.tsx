'use client';
import {useState} from 'react';import {accounts} from './data';import {Facts,Badge,AccountDialog,Rules} from './shared';
export default function Brief(){const [index,setIndex]=useState(0);const a=accounts[index];return <main className="brief-page"><div className="brief-heading"><div className="eyebrow">SATURDAY, SEPTEMBER 19 · PROPERTY</div><Rules/></div><h1>Three accounts worth<br/>a closer look.</h1><p className="brief-intro">Your queue is reviewed. Three accounts meet appetite.<br/>One needs evidence. One falls outside the guidelines.</p><div className="brief-grid"><section className="hero-account"><div className="section-label"><span>PURSUIT · {index+1} OF 3</span><Badge a={a}/></div><h2>{a.name}</h2><p>{a.reason}</p><Facts a={a}/><div className="hero-bottom"><AccountDialog a={a} label="Start account review"/><span>{a.targets.filter(Boolean).length} of 4 target preferences</span></div><div className="brief-switch">{accounts.slice(0,3).map((x,i)=><button key={x.id} aria-label={`Show ${x.name}`} aria-pressed={i===index} onClick={()=>setIndex(i)}><span>0{i+1}</span>{x.name}</button>)}</div></section><aside className="brief-aside"><div className="eyebrow">ALSO IN YOUR QUEUE</div><div className="aside-case"><Badge a={accounts[3]}/><h3>Northline Logistics</h3><p>Potential target fit, but loss history is missing.</p><AccountDialog a={accounts[3]} label="Resolve missing evidence"/></div><div className="aside-case"><Badge a={accounts[4]}/><h3>Westhaven Commerce</h3><p>Premium exceeds the appetite maximum.</p><AccountDialog a={accounts[4]} label="View exclusion"/></div></aside></div><p className="caption">All 5 submissions shown · Proposed rubric v0.1 · Recommendations for human review</p></main>}
