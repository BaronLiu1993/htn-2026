'use client';
import {useState} from 'react';
import {accounts} from './data';
import {Filters,Badge,AccountDialog,Rules} from './shared';
export default function Queue(){const [group,setGroup]=useState('Pursue');const [search,setSearch]=useState('');const rows=accounts.filter(a=>a.group===group&&a.name.toLowerCase().includes(search.toLowerCase()));return <main className="queue-page"><div className="page-heading"><div><div className="eyebrow">YOUR WORKSPACE</div><h1>Submission queue</h1><p className="muted">A clear next step for every account.</p></div><Rules/></div><div className="queue-summary"><span><i/>5 of 5 submissions accounted for</span><span>3 eligible · 1 unresolved · 1 excluded</span></div><div className="toolbar"><Filters value={group} onChange={setGroup}/><input aria-label="Search accounts" placeholder="Search accounts…" value={search} onChange={e=>setSearch(e.target.value)}/></div><div className="table-wrap"><table><thead><tr><th>Account / reason</th><th>Appetite</th><th>Target fit</th><th>Premium</th><th>Next step</th></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td><strong>{a.name}</strong><small>{a.state} · ${a.tiv}M insured value · Built {a.year}</small><p>{a.reason}</p></td><td><Badge a={a}/></td><td>{a.group==='Pursue'?<><strong>{a.targets.filter(Boolean).length} of 4</strong><small>target preferences</small></>:<span className="muted">Not scored</span>}</td><td>${a.premium}K</td><td><AccountDialog a={a} label={a.group==='Investigate'?'Resolve gap':a.group==='Out of appetite'?'View exclusion':'Review account'}/></td></tr>)}</tbody></table>{rows.length===0&&<p className="empty">No accounts match “{search}”. Try another name.</p>}</div><div className="footnote"><span>Property · New business · USD</span><span>Fit orders eligible accounts. It does not predict profitability.</span></div>{group==='Pursue'&&<button className="investigation-callout" onClick={()=>setGroup('Investigate')}><span className="amber-dot">!</span><span><strong>One promising account needs evidence</strong><small>Northline Logistics is missing five-year loss history.</small></span><span>Investigate →</span></button>}</main>}
