# Agent instructions

This repo is **UnderwriteIQ** (Hack the North 2026 / Federato).

1. Read `.cursor/rules/underwriteiq-context.mdc` (always applied in Cursor).
2. Read `UNDERWRITING_AGENT_HANDOFF.md` before implementing — it is the current task list imported from Codex.
3. Respect `backend/AGENTS.md` and `frontend/AGENTS.md` when editing those trees.
4. Do not add backend tests; do not use Playwright; do not push unless asked.
5. Never commit secrets; credentials belong in `.env` only.
